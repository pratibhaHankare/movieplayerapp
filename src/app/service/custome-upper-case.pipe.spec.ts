import { CustomeUpperCasePipe } from '../custome-upper-case.pipe';

describe('CustomeUpperCasePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomeUpperCasePipe();
    expect(pipe).toBeTruthy();
  });
});
